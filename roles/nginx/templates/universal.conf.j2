server {
    server_name {{ inventory_hostname }};
    listen 443 ssl;

    ssl_certificate "/etc/pki/nginx/sapkyy.home.crt";
    ssl_certificate_key "/etc/pki/nginx/sapkyy.home.key";
    ssl_session_cache shared:SSL:1m;
    ssl_session_timeout  10m;

    client_max_body_size 100M;

    access_log /var/log/nginx/{{ inventory_hostname }}_access.log;
    error_log  /var/log/nginx/{{ inventory_hostname }}_error.log;

    location / {
      proxy_pass {{ upstream_scheme }}://127.0.0.1:{{ proxy_port }};
      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-for $remote_addr;
      port_in_redirect off;
      proxy_redirect {{ upstream_scheme }}://127.0.0.1:{{ proxy_port }}/ /;
      proxy_connect_timeout 600;
    }
}
